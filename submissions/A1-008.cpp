#include <bits/stdc++.h>

using namespace std;

int main(){
       string n;
       cin>>n;
       int length = n.length();
       if(length==13) cout<<"yes";
       else cout<<"no";
       return 0;
}